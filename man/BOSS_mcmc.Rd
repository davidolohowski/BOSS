% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_stage1.R
\name{BOSS_mcmc}
\alias{BOSS_mcmc}
\title{Bayesian Optimization via Sequential MCMC (In Development)}
\usage{
BOSS_mcmc(
  func,
  update_step = 10,
  max_iter = 50,
  inner_iter = 10,
  MCMC_size = 10000,
  D = 1,
  lower = rep(0, D),
  upper = rep(1, D),
  nu = Inf,
  quad = FALSE,
  noise_var = 1e-06,
  modal_k.nn = 5,
  initial_design = 10,
  delta = 0.01,
  alpha = 0.05,
  acc = 0.25,
  explore_size = 500,
  optim.n = 1,
  optim.max.iter = 1000,
  Rhat_eps = 0.01,
  UCB_prob = 0.1,
  verbose = 1
)
}
\arguments{
\item{func}{A function \code{f: [0,1]^D -> R} to optimize.}

\item{update_step}{Integer; how often to refit the GP hyperparameters.}

\item{max_iter}{Maximum number of BO iterations.}

\item{inner_iter}{Integer; how often to perform MCMC sampling based on the current surrogate}

\item{MCMC_size}{Integer; MCMC iterations}

\item{D}{Input dimension.}

\item{lower}{Numeric vector of length D giving lower bounds (in original scale).}

\item{upper}{Numeric vector of length D giving upper bounds.}

\item{nu}{GP covariance smoothness. \eqn{\nu = \infty} represents squared-exponential kernel;
\eqn{3 < \nu < \infty} represents the Matern kernel with smoothness \eqn{\lceil\nu\rceil - 1/2}.
\eqn{\nu > 3} is required to ensure convergence properties.}

\item{quad}{Logical; if \code{FALSE} (default), use a zero-mean GP model.
If \code{TRUE}, fit a linear-plus-quadratic mean model
\eqn{\beta_0 + x^\top \beta_1 + (x \otimes x)^\top \beta_2} alongside the GP.}

\item{noise_var}{GP observation noise variance.}

\item{modal_k.nn}{Number of nearest neighbours to compute modal covering.}

\item{initial_design}{Number of initial points to sample.}

\item{delta}{Exploration parameter passed to UCB.}

\item{alpha}{Numeric value between \code{(0,1)}. Used to determine new search region based on current MCMC sample.}

\item{acc}{Numeric; accuracy required of the modal exploitation by BO.}

\item{explore_size}{Integer; how many MCMC sample to use to explore location with the highest surrogate uncertainty.}

\item{optim.n}{Number of multistart in acquisition optimization.}

\item{optim.max.iter}{Maxit for length-scale optimization.}

\item{Rhat_eps}{Numeric; required accuracy to terminate the algorithm based on the \eqn{\hat{R}} statistics between successive MCMC samples.
Default to \code{Rhat_eps = 0.01} to represent a stopping criterion of \eqn{\hat{R} < 1.01}.}

\item{UCB_prob}{Numeric; probability threshold to indicate the modal convergence from UCB. Default to \code{UCB_prob < 0.1}.}

\item{verbose}{Verbosity level (0–3).}
}
\value{
A "boss_mcmc" object (S3 class \code{boss_mcmc}), which is a list containing:
  \describe{
    \item{\code{objective_function}}{The original objective function \code{f}.}
    \item{\code{D}}{Input dimension of the objective function.}
    \item{\code{design_points}}{List with components \code{x} (scaled inputs), \code{x_original} (original inputs), and \code{y} (responses).}
    \item{\code{gp_params}}{List with \code{length_scale} and \code{signal_var}, the final GP hyperparameters.}
    \item{\code{surrogate}}{A function \code{(xvalue) -> numeric} giving the GP posterior mean at \code{xvalue}.}
    \item{\code{essential_support}}{A box specifying the essential support identified by the last MCMC sample.}
    \item{\code{fill_in}}{Placeholder for additional outputs (to be computed later).}
    \item{\code{mcmc_result}}{List with components \code{sample} (MCMC sample from the last iteration), \code{R_hat} (\eqn{\hat{R}} statistics across MCMC-sampling phase.) }
    \item{\code{modal_result}}{Data frame of modal-distance diagnostics collected during optimization.}
  }
}
\description{
Run a GP-based sequential MCMC algorithm that:
\enumerate{
  \item Re-estimates GP length-scale/noise;
  \item Alternate between performing BO (\code{inner_iter} steps) and perform MCMC sampling on the current GP surrogate to update BO search region;
  \item Algorithm terminates when successive MCMC samples indicate convergence (by \eqn{\hat{R}} statistics) and BO has reached modal convergence.
}
}
