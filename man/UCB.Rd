% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_utility.R
\name{UCB}
\alias{UCB}
\title{Upper Confidence Bound (UCB) Acquisition Function}
\usage{
UCB(x, data, cov, nv, D, d, quad = FALSE)
}
\arguments{
\item{x}{Numeric vector or matrix of candidate input locations.}

\item{data}{A list with observed GP data, containing elements \code{x} (inputs) and \code{y} (responses).}

\item{cov}{A covariance-generating function (e.g., returned by \code{\link[BOSS]{cov_generator}}).}

\item{nv}{Numeric scalar. Observation noise variance.}

\item{D}{Integer. Total number of GP training points.}

\item{d}{Integer. Dimensionality of the input space.}

\item{quad}{Logical; passed to \code{\link[BOSS]{predict_gp}}. If \code{TRUE}, uses a quadratic mean model.}
}
\value{
Numeric vector of UCB values (higher is better). Internally returns \(-\mu(x) - \sqrt{\beta\,\Sigma(x)}\)
  so that minimizing this corresponds to maximizing the classic UCB acquisition \(\mu + \sqrt{\beta\,\sigma^2}\).
}
\description{
Compute the UCB acquisition value (to be maximized) at candidate inputs for Bayesian optimization.
}
\details{
The exploration–exploitation tradeoff parameter \(\beta\) is set as
\deqn{\beta = 2\log\bigl(D^2\pi^2/(6\,d)\bigr).}
The function calls \code{\link[BOSS]{predict_gp}} to obtain the posterior mean \(\mu(x)\)
and variance \(\sigma^2(x)\), then computes
\[
  \mathrm{UCB}(x) \;=\; \mu(x) \;+\;\sqrt{\beta\,\sigma^2(x)}.
\]
}
\examples{
# Not run
# cov_fn <- cov_generator(length_scale = 1, signal_var = 1, nu = Inf)
# data <- list(x = matrix(runif(20), ncol = 2),
#               y = rnorm(10))
# UCB(seq(0,1,length.out=5), data, cov_fn, nv = 1e-3, D = 10, d = 2)

}
